# Momentus AI Backend
# Python 3.9+

# Web framework — bumped to versions compatible with anyio>=4.5
# (required by mcp>=1.2.0). fastapi 0.115 + httpx 0.27 + anyio 4 form a
# coherent stack with pydantic 2.x.
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# Database
aiosqlite==0.20.0

# HTTP client (async) — 0.27+ supports anyio 4
httpx==0.27.2

# Async runtime (pinned so we never regress against mcp>=1.2.0)
anyio>=4.5,<5

# HTML parsing & cleaning
beautifulsoup4==4.12.3
lxml==5.3.0
readability-lxml==0.8.1

# Claude API (0.40+ supports the new anyio)
anthropic==0.42.0

# Data validation
pydantic==2.10.4
pydantic-settings==2.7.0

# CSV handling (stdlib but explicit)
# csv — built-in

# Utilities
python-dotenv==1.0.0
tenacity==8.2.3

# Model Context Protocol (MCP) — Peec.ai live sync + Momentus MCP server
# Requires Python 3.10+
mcp>=1.2.0
